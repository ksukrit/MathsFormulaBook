<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowActivityTransitions">true</item>

        <item name="colorPrimary">?themePrimary</item>
        <item name="colorPrimaryDark">?themePrimaryDark</item>
        <item name="colorAccent">?themeAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

    </style>

    <style name="AppTheme.DarkTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="themeContentTextColor">@color/colorAccentDark</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="themePrimary">@color/greenA400</item>
        <item name="themePrimaryDark">@color/greenA700</item>
        <item name="themeAccent">@color/colorAccentgreen</item>
        <item name="themeContentBackground">@color/white</item>
        <item name="themeContentTextColor">@android:color/darker_gray</item>
        <item name="themeContentTextColoOnPrimary">@color/white</item>
        <item name="themeTextColorSuccess">@color/greenA700</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
